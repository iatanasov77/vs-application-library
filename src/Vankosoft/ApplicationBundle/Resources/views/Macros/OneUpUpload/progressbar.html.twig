{% macro jquery_ui( id, resumeButton = null ) %}
    <div id="{{ id }}" class="progressBar">
        <div class="progressInfo"><span class="caption"></span></div>
    </div>
{% endmacro %}

{% macro easy_ui( id, resumeButton = null ) %}
    <div id="{{ id }}" class="progressBar"></div>
    
    {#
    <div class="row">
        <div class="col-11">
            <div id="{{ id }}" class="progressBar"></div>
        </div>
        {% if resumeButton %}
            <div class="col-1">
                <a href="javascript:void( 0 );" id="{{ resumeButton }}" class="easyui-linkbutton"></a>
            </div>
        {% endif %}
    </div>
    #}
{% endmacro %}
